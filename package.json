{
  "name": "travel-buddy",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "start": "yarn workspace backend start",
    "dev": "yarn workspace backend dev",
    "build": "yarn workspace frontend build"
  }
}
